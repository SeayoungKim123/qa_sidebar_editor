# 메일 AI 사이드패널 — 컨텐츠 일부 요소 변경하기 (SCN-02) — 실행 결과

**RUN-ID**: RUN-20260609-1859-dev
**환경**: dev
**시간**: 19:22:58 ~ 19:29:20 (약 6m, TC-01~03)
**결과**: ✅ 3 Pass / ❌ 0 Fail / — TC-04 미실행(사용자 중단)
**모델**: Claude Sonnet 4.6

## 사용자 흐름
SCN-01 산출물(완성된 신제품 출시 안내 메일)을 substrate 로 재사용. 여러 문단 메일에서 특정 요소(한 문단·한 표현·한 문장)만 콕 집어 수정하고 나머지는 무변동인지 검증. 새 대화로 시작(본문 유지).

## 진입 / 측정 노트
- PRE-FLIGHT 측정 통과: URL ✓ / 메일 LNB left=64(접힘) ✓ / `#ai-chat-iframe-panel` ✓ / 무 다이얼로그 / 모델 Claude Sonnet 4.6 ✓.
- **문단 "드래그 선택" 한계**: 메일 에디터는 `designMode` iframe 으로, `browser_evaluate` 의 Selection API 로 문단을 선택해도 `toString()`=0 으로 선택이 잡히지 않아 AI 에 전달되지 않았다. → 시나리오 사용자 흐름이 허용한 **"명시적 지정"**(프롬프트에 대상 문단 첫 구절 인용)으로 대체. 동일 검증(한 요소만 수정·나머지 무변동) 가능.

## TC-01: 특정 문단 톤 변환 — ✅ PASS
- 입력: 대상 문단 명시 지정 + "더 공손하고 정중한 톤으로 바꿔줘. 다른 문단은 그대로"
- 동작: AI `convert_tone` → `search_editor_text` → `replace_editor_block` 로 해당 블록만 교체
- 결과: "직접 제품을 경험해 보실 수 있도록…" 문단이 "부장님께서…정성껏 마련해 드리고자 합니다. 바쁘신 일정 중에 이렇게 요청드리게 되어 송구스럽사오나…신속히 준비해 드리겠습니다."로 격상
- 검증: 의미 유지 ✓ / 톤 상승 체감 ✓ / **나머지 문단·표 글자 단위 무변동** ✓
- AI 작업시간 ~59s. 토큰 in 9,184 / out 2,053.
- 스크린샷: `screenshots/mail-partial-tc01-02-prompt.png`, `mail-partial-tc01-03-after.png`

## TC-02: 특정 표현 일괄 치환 — ✅ PASS (단, 치환 동작 자체는 미검증)
- 입력: "'할인'이라는 단어를 모두 '특별 혜택'으로 바꿔줘"
- 동작: AI `search_editor_text('할인')` → 결과 없음 → "본문에 '할인'이 존재하지 않습니다" 안내, **본문 무변경**
- 검증: '할인' 잔존 0 ✓ / 다른 단어 변형 없음 ✓ / **없는 단어를 환각으로 만들어내지 않음** ✓
- 비고: substrate(SCN-01 산출물)에 '할인'이 없어 **일괄 치환 동작 자체는 검증되지 않음**. AI 의 no-match 처리는 정확. 향후 '할인' 포함 더미 메일로 치환 폭 재검증 권장.
- AI 작업시간: 측정 오염(스냅샷 분석이 t_done 앞에 끼어듦)으로 `null` 기록(체감 ~15s 내). 토큰 in 4,374 / out 287.
- 스크린샷: `screenshots/mail-partial-tc02-02-prompt.png`, `mail-partial-tc02-03-after.png`

## TC-03: 한 문단만 요약 — ✅ PASS
- 입력: 가장 긴 문단(TC-01 에서 길어진 데모 문단) 명시 지정 + "핵심 의미 유지한 채 한 문장으로 짧게"
- 결과: 2문장 → 1문장 "편하신 시간을 알려 주시면 데모 시연 및 미팅 자리를 신속히 마련해 드리겠습니다."로 축약
- 검증: 문단 길이 감소 ✓ / 핵심 의미 보존 ✓ / **다른 문단 무변동** ✓
- AI 작업시간 ~51s. 토큰 in 5,235 / out 1,105.
- 스크린샷: `screenshots/mail-partial-tc03-02-prompt.png`, `mail-partial-tc03-03-after.png`

## TC-04: 한 문장 맞춤법 교정 — — 미실행
- 사용자 요청으로 RUN 중단. substrate 에 오타 문장이 없어 "AI 로 오타 포함 문장 추가(셋업) → 해당 문장만 교정" 흐름을 진행하려다 중단됨. 다음 회차 재개 권장.

## 핵심 관찰
- **부분 편집 정밀도 양호**: TC-01·03 모두 지정 요소만 수정하고 나머지 본문·표를 글자 단위로 보존(번짐 0). `search_editor_text` + `replace_editor_block` 조합으로 정확히 스코프.
- **no-match 안정성 양호(TC-02)**: 없는 단어 치환 요청에 환각 없이 정확히 무변경.
- **드래그 선택 미전달**: designMode iframe 선택이 AI 로 전달되지 않음 — 선택 기반 UX 검증은 실제 마우스 드래그 경로 필요(스킬 보강 후보).

## 정책 참조
- `specs/` — 메일 AI 어시스턴트 사용 가이드(톤 변환·맞춤법), 본문 에디터 서식 정책
