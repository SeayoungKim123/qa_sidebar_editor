# 메일 AI 사이드패널 — 백지에서 컨텐츠 채우기 (SCN-01) — 실행 결과

**RUN-ID**: RUN-20260609-1744-dev
**환경**: dev
**시간**: 17:45 ~ 17:54 (약 9m, 패널 진입 시행착오 포함)
**결과**: ❌ 0 Pass / ❌ 1 Fail (TC-01) — **TC-01 단독 재실행**

> 이번 실행은 TC-01 단독 재실행 요청이다. TC-02~04 는 실행하지 않았다.

## 진입 경로·환경
- 로그인: `login.devoffice.hiworks.com` 2단계(ID→비번), 계정 `hakyoung@joy1.com`. 진입 시 "보안을 위하여 다시 로그인" 다이얼로그 → 재로그인.
- 메일 쓰기: `mails.devoffice.hiworks.com/write?mode=normal` 직접 진입. 첫 진입 시 "인앱마케팅 모달" 닫음.
- **LNB 축소(중요)**: 메일 좌측 메일함 LNB 우측 끝 드래그 핸들(`DragResizable_drag`)을 **더블클릭**해 LNB 를 아이콘 레일로 축소(`environments/dev.md` 진입 절차). 이 단계를 생략하면 패널 폭이 좁아 AI 채팅 좌측 사이드바(채팅기록)가 backdrop 딸린 **모바일 드로어**로 펼쳐져 채팅 영역을 가린다 — 이번에 재확인.
- AI 패널: 우측 상단 보라색 "AI 채팅 패널 토글" → 우측 iframe 패널(폭 400px). "작성 중인 메일 참조 중" 배지·추천기능 칩 12종 확인. 모델 기본값 **Claude Sonnet 4.6**.
- 전송: 입력창 포커스 후 **Enter**.

## TC-01: 메일 초안 작성 (백지) — ❌ FAIL
- 입력: 빈 본문, 프롬프트 `"거래처에 신제품 출시를 안내하는 비즈니스 메일 초안을 작성해줘. 출시일과 핵심 가치를 포함해서"`
- 단계별 결과:
  1. 본문 클릭(커서) → 프롬프트 Enter 전송 ✓
  2. AI 가 `get_mail_draft` 로 빈 본문 확인 후, **본문에 초안을 넣는 대신 "신제품 출시 안내 메일 정보 입력" 폼을 노출** — 수신자·신제품명·출시일(AI 추천 2026-07-01)·핵심 가치(필수)·요청 사항(선택) 입력을 요구. (1차 응답 토큰 input 54,840 / output 1,020)
  3. 추가 정보 없이 AI 가 스스로 작성하도록 **"건너뛰기"** 클릭 → AI 가 이를 **"취소"로 해석**: "네, 취소하셨군요! … 초안이 필요하시면 아래 정보를 알려주시면 바로 작성해 드리겠습니다" 안내만 출력, **본문 변동 없음**. (2차 응답 토큰 input 39,018 / output 209)
- 기대: 본문에 한국어 비즈니스 메일 초안 삽입 (150자 이상, 신제품/출시 맥락, 깨짐 없음)
- 실제: ❌ **본문이 끝까지 빈 상태**. AI 가 맨 프롬프트만으로는 초안을 생성하지 않고 필수 정보 입력 폼을 요구하며, 건너뛰기 시 생성이 취소됨. 검증 포인트(본문 150자 이상) 미충족.
- **직전 회차(RUN-20260609-1536-dev) 대비 동작 변화**: 1536 회차에서는 동일 프롬프트에 AI 가 568자 초안을 `[담당자명]`·`[출시일 입력]` 플레이스홀더로 **즉시 자동 삽입**해 PASS 였다. 이번 회차는 플레이스홀더 자동 작성 대신 **정보 입력 폼 게이트**로 바뀌어, 폼을 채우지 않으면(또는 건너뛰면) 초안이 생성되지 않는다.
- 추정 원인: 메일 초안 스킬이 "필수 정보 수집 폼 우선" 흐름으로 변경된 것으로 보임. 폼의 "초안 작성하기" 경로(수신자·신제품명·핵심가치 입력)는 본 시나리오 프롬프트가 해당 값을 주지 않아 미검증 — 본문 위조 방지를 위해 임의 값으로 채우지 않음.
- wait_ms: null (본문 편집 완료 시점이 없음 — 초안 미생성)
- 토큰: 1차(프롬프트 응답) input 54,840 / output 1,020 으로 기록
- 스크린샷: `screenshots/mail-tc01-02-prompt.png`(전송 전 빈 본문+프롬프트), `mail-tc01-03-after.png`(건너뛰기 후 빈 본문+취소 안내), `mail-tc01-03b-form-gate.png`(정보 입력 폼 게이트)

## 관찰·권장
- **재현 필요**: 백지 초안 작성이 "자동 작성(플레이스홀더)"에서 "정보 입력 폼 필수"로 바뀐 것이 의도된 변경인지 확인 권장. 의도된 변경이면 SCN-01 시나리오의 TC-01 검증 기준(맨 프롬프트→본문 초안)을 폼 경로 기준으로 갱신해야 한다.
- "건너뛰기"의 의미가 "정보 없이 작성"이 아니라 "작성 취소"이므로, 사용자가 빠르게 초안만 원할 때 막힌다 — UX 확인 권장.
- 폼 "초안 작성하기" 경로(필수 필드 입력 후)의 실제 본문 생성 품질은 별도 TC 로 검증 권장.

## 정책 참조
- `specs/` — 메일 AI 어시스턴트 사용 가이드 (미작성)
