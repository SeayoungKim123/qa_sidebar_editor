# DOCX 실사용 흐름 — 백지에서 작성하고 다듬기 — 실행 결과

**RUN-ID**: RUN-20260602-1318-dev
**환경**: dev
**시간**: 약 13:23:17 ~ 13:33:33 (≈10m, 환경 초기화·UI 재시도 오버헤드 포함)
**결과**: ✅ 3 Pass / ❌ 0 Fail (샘플 시험 — 1단계 TC-01~03만 실행, TC-04 이후 미실행)

## 사용자 흐름
빈 DOCX 에서 시작해 ① 개요 3문단 → ② 1·2·3 섹션(제목+본문2문단) → ③ 추진일정 아래 일정표를
AI 사이드패널 프롬프트만으로 단계적으로 생성. 본문 직접 편집 없이 AI 가 문서를 편집하는지 검증.

> **사전 조건 복원 메모**: 진입 시 대상 문서(`새 문서.docx`)에 직전 회차(RUN-20260602-1157-dev)
> 산출물(개요·섹션 등)이 남아 있었음 — 커넥터가 변경분을 자동저장(상태바 "모든 변경 사항이 저장되었습니다")
> 하기 때문. 시나리오 사전조건이 **빈 DOCX** 이므로, AI 작업을 위조하지 않는 범위에서 본문 전체 선택→삭제로
> 백지 상태만 복원한 뒤 TC-01 부터 진행함(타이핑·서식 등 AI 가 해야 할 작업은 일절 하지 않음).

## TC-01: 개요 단락 작성 — ✅ PASS
- 입력: 빈 문서 맨 위 커서 + 프롬프트("가비아 2026 사업계획서 개요를 3문단으로 …")
- 단계별 결과:
  1. 본문 맨 위 커서 지정 ✓
  2. 프롬프트 전송 → AI `create_document` → `get_document_overview`(빈 문서 확인) → `insert_document_blocks` ✓
  3. 회사 소개 / 2026 방향성 / 기대효과 3문단 삽입, 200자 훨씬 상회·단락 구분 명확·깨짐 없음 ✓
- 토큰: input 1,663 / output 1,565
- 비고: 전송 버튼이 사이드바 드로어 backdrop 에 가로막혀(아래 관찰) 재시도가 길어진 탓에 실제 전송 시점이
  `t_send` 측정값보다 밀려 **wait_ms 는 null**(오염) 처리. 결과 판정에는 영향 없음.
- 스크린샷: `screenshots/01-docx-tc01-01-before.png`, `…-02-prompt.png`, `…-03-after.png`

## TC-02: 섹션 구조 생성 — ✅ PASS
- 입력: 개요 끝 커서 + 프롬프트("개요 다음에 '1. 시장환경','2. 사업전략','3. 추진일정' …")
- 단계별 결과:
  1. 커서 문서 끝으로 이동 ✓
  2. AI `manage_cursor`(끝 이동) → `insert_document_blocks` ✓
  3. 제목 3개(1./2./3.) + 각 본문 2문단 순서대로 삽입, 제목·본문 시각 구분 명확, 섹션 순서 정확 ✓
  4. TC-01 개요 단락 변형 없음 ✓
- AI 작업 시간(wait_ms): 72s · 토큰: input 3,308 / output 1,516
- 스크린샷: `screenshots/01-docx-tc02-01-before.png`, `…-02-prompt.png`, `…-03-after.png`

## TC-03: 표 삽입 — ✅ PASS
- 입력: 추진일정 본문 끝 커서 + 프롬프트("추진일정 섹션 아래에 분기별 일정표를 … 4행 3열 …")
- 단계별 결과:
  1. 커서 문서 끝으로 이동 ✓
  2. AI `table_management` → `get_document_overview` → `insert_table` → `format_table` ✓
  3. 4행 3열 표 삽입, 첫 행 헤더(분기/과제/담당) + 1Q~3Q 더미 3행, 헤더 음영·테두리 적용 ✓
  4. 타 섹션 변형 없음 ✓
- AI 작업 시간(wait_ms): 56s · 토큰: input 6,497 / output 1,160
- 스크린샷: `screenshots/01-docx-tc03-01-before.png`, `…-02-prompt.png`, `…-03-after.png`

## 이번 회차 관찰
- **AI 1단계(백지 작성) 안정 재확인** — 3개 TC 모두 적절한 도구(`insert_document_blocks`·`manage_cursor`·`insert_table`·`format_table`) 호출로 정확히 수행. GPT 5.4 모델.
- **에디터 첫 로드 CORS 차단 미발생** — 이번 회차는 `문서편집` 진입 직후 정상 로드(콘솔 에러 0, ISS-004 미재현).
- **신규 관찰 — AI 채팅 사이드바 드로어 backdrop 이 전송 클릭 가로막음**: 좁은 패널에서 사이드바 토글 시 모바일 드로어(backdrop `fixed inset-0 z-50`)가 떠 전송 버튼 클릭이 차단됨. Escape 로 드로어를 닫고 입력창 포커스 상태에서 **Enter 로 전송**하면 정상 동작. TC-02·03 은 Enter 전송으로 wait_ms 깔끔히 측정됨.
- **자동저장으로 직전 회차 산출물 잔존** — 시험 모드(저장 버튼 비활성)인데도 커넥터가 변경분을 자동저장해, 빈 문서 사전조건을 매 회차 복원해야 함.

## 정책 참조
- `specs/` — AI 어시스턴트 사용 가이드 / DOCX 서식·표·스타일 정책
