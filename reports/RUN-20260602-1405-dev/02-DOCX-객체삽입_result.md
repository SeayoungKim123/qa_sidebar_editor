# 02-DOCX-객체삽입 — 실행 결과

**RUN-ID**: RUN-20260602-1405-dev
**환경**: dev
**시간**: 14:53 ~ 15:09 (약 16m)
**결과**: ✅ 2 Pass / ❌ 5 Fail (TC-05 BLOCKED 포함)
**대상 파일**: `새 문서.docx` (빈 DOCX로 복원 후 시작), 모델 `GPT 5.4`

## 사용자 흐름
빈 문서에서 ① 기준 표 생성 → ② 표 구조 편집(행/병합·열) → ③ 차트 삽입·종류변경 → ④ 도형·로고 → 저장/영속. **객체 삽입·구조 편집 축**을 전담. 핵심 결론: **DOCX AI 사이드패널은 표 생성/셀 텍스트·표 서식은 가능하나, 셀 병합·행 추가·열 추가·차트·도형의 네이티브 도구가 부재**해 텍스트/표 우회로만 대응한다.

## TC-01: 기준 표 생성 — ✅ PASS
- `insert_table`로 4행 3열 표(헤더 분기/과제/담당 + 1Q~3Q 더미: 시장조사/김민수, 시제품개발/이서연, 출시준비/박준호) 정확 생성. 헤더 행 구분.
- 스크린샷: `screenshots/02-docx-tc01-03-after.png`

## TC-02: 행 추가 + 셀 병합 — ❌ FAIL
- 4Q 행: `add_table_row` 2회 시도 모두 `unknown_error`로 실패 → 미반영.
- 3열 병합 제목 셀: **셀 병합 도구 자체가 없음** → 미지원.
- 둘 다 적용 안 됨, 표 미변경. AI는 우회안(제목 문단/제목 행)만 제시.
- 스크린샷: `screenshots/02-docx-tc02-03-after.png`

## TC-03: 열 추가 — ✅ PASS (우회)
- 네이티브 열 추가 도구가 없어 `delete_text_blocks`로 기존 표 삭제 후 `insert_table`로 4열 표 재삽입.
- 결과: 분기/과제/담당/**진행률**(데이터행 0%), 기존 3열 데이터 보존. 시각 결과는 요청대로.
- 비고: 파괴적 재생성 우회 — 표가 크거나 서식이 복잡하면 위험.
- 스크린샷: `screenshots/02-docx-tc03-03-after.png`

## TC-04: 차트 삽입 — ❌ FAIL
- **차트 삽입 전용 도구 없음.** AI가 `█` 블록문자로 된 텍스트 ASCII 막대차트("1Q | ███ 1" …)를 `insert_document_blocks`로 삽입.
- 실제 차트 개체가 아니라 더블클릭 데이터 편집 불가 → 기대(막대 차트) 미충족.
- 스크린샷: `screenshots/02-docx-tc04-03-after.png`

## TC-05: 차트 종류 변경 — ❌ BLOCKED
- TC-04에서 실제 차트가 생성되지 않아(텍스트만 존재) 변경 대상 차트 부재 → 검증 불가.

## TC-06: 강조 도형 삽입 — ❌ FAIL
- **도형 삽입 전용 도구 없음.** 빨간 배경+흰 '초안' 텍스트의 **1칸 표**(각진 모서리)로 대체.
- 둥근 사각형 도형 객체 아님 → 기대 미충족.
- 스크린샷: `screenshots/02-docx-tc06-03-after.png`

## TC-07: 로고 자리표시 도형 + 저장/영속 — ❌ FAIL
- 회색 사각형 도형 대신 박스문자 3줄(가운데 정렬, 회색, '로고') **텍스트 박스**로 대체 → 도형 요건 미충족.
- **저장/영속은 양호**: 재오픈 후 로고 박스·초안 라벨·4열 표(+데이터)·텍스트 차트 **전부 영속**(소실 없음). 커넥터 자동저장 동작.
- 스크린샷: `screenshots/02-docx-tc07-03b-after-reopen.png`

## 핵심 이슈 (ISS-001 연계)
- **DOCX 객체/구조 편집 도구 부재**: 셀 병합·행 추가(불안정)·열 추가·차트·도형 네이티브 미지원. AI는 텍스트/표 우회로 "비슷하게" 만들지만 실제 개체가 아니어서 후속 편집(차트 종류 변경 등)이 불가.
- `add_table_row` `unknown_error` 간헐 실패(2회).
- 영속성 자체는 정상.

## 정책 참조
- `specs/` — AI 어시스턴트 지원 객체·명령 범위, DOCX 표·차트·도형 정책
