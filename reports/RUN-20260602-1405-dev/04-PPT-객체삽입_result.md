# 04-PPT-객체삽입 — 실행 결과

**RUN-ID**: RUN-20260602-1405-dev
**환경**: dev
**시간**: 15:26 ~ 15:29
**결과**: ❌ 0 Pass / 6 Fail (TC-01 FAIL, TC-02~06 BLOCKED)
**대상 파일**: `새 문서.pptx` (빈 PPTX), 모델 `GPT 5.4`

## 핵심 결론
03-PPT-실사용흐름에서 확인된 **PPT 열린 덱 실시간 편집 불가**가 객체 삽입 축에서도 동일하게 재현되었다. 표·차트·도형을 열린 슬라이드에 삽입하는 전제 자체가 성립하지 않는다.

## TC-01: 매출표 슬라이드 생성 — ❌ FAIL
- "분기별 매출표 슬라이드" 요청 → 방식 선택 UI(Native/HTML) → **Native 선택**.
- AI는 또다시 OnlyOffice 열린 덱을 편집하지 못하고 샌드박스에서 별도 PPTX 파일 생성 경로로 진입. **열린 슬라이드는 1장 placeholder 그대로**(표 미삽입).
- 토큰 input 9.8k / output 808 (방식 선택 직전까지).
- 스크린샷: `screenshots/04-ppt-tc01-03-after.png`

## TC-02 ~ TC-06 — ❌ BLOCKED
- TC-01에서 매출표 슬라이드가 열린 덱에 생성되지 않아, 후속 TC의 대상(표·차트·도형)이 부재 → 검증 불가.
  - TC-02 셀병합/행추가 / TC-03 차트 슬라이드 / TC-04 차트 종류변경 / TC-05 강조 도형 / TC-06 저장·영속.

## 권장 액션
- 03번 결과와 동일: **OnlyOffice 프레젠테이션 열린 문서에 대한 native 객체(표/차트/도형) 삽입 도구 연결** 필요. 현재는 샌드박스 파일 생성만 가능.
- DOCX 축(01/02)은 텍스트·표·서식이 열린 문서에 즉시 반영되는 반면, PPT 축(03/04)은 열린 덱 반영 경로가 전무 — 엔진별 도구 셋 격차가 큼.

## 정책 참조
- `specs/` — PPTX 표·차트·도형 정책
