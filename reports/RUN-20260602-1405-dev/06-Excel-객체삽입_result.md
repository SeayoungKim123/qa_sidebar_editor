# 06-Excel-객체삽입 — 실행 결과

**RUN-ID**: RUN-20260602-1405-dev
**환경**: dev
**시간**: 15:56 ~ 16:12 (약 16m)
**결과**: ✅ 6 Pass / ❌ 1 Fail
**대상 파일**: `새 문서.xlsx` (시트1: 새 빈 워크시트), 모델 `GPT 5.4`

## 핵심 결론
Excel 객체 축은 **표·SUM·막대/원형/꺾은선 3종 차트 생성·행 삽입+셀 병합·수식 참조 자동 보정·재오픈 영속**까지 모두 정상. 유일한 미지원은 **차트 스타일 편집(색/축/격자선)** 으로 05-Excel TC-09와 동일 한계.

## TC-01: 매출표 + 총합 수식 — ✅ PASS
A1:E4 표 + E열 `=SUM(B2:D2)`. AI가 get_cell_formula로 수식 잔존까지 자가 검증.

## TC-02: 막대 차트 — ✅ PASS
`create_chart`로 막대 차트('1분기 매출') 생성.

## TC-03: 원형 차트 — ✅ PASS
원형 차트('직원별 매출 비중', 3조각). 막대 차트와 공존. (AI가 보조 데이터 영역 생성해 비중 산출)

## TC-04: 꺾은선 차트 — ✅ PASS
꺾은선 차트('직원별 1~3월 매출 추이', 직원 3선, 가로축 월). 막대·원형과 함께 **차트 3개 공존**.

## TC-05: 차트 디자인 수정 — ❌ FAIL
색상 파랑/축 12pt/격자선 제거 요청 → AI가 **"차트 스타일 편집 미지원, 차트 생성까지만 가능"** 명시. 3항목 미반영. (05 TC-09와 동일)

## TC-06: 행 삽입 + 셀 병합 (수식·차트 참조 보정) — ✅ PASS
- 최상단에 '1분기 매출 현황' 제목 행 삽입, A~E 병합. 기존 헤더/데이터 한 행씩 하향.
- **핵심: SUM 수식 참조 자동 보정** — E3 수식 입력줄 `=SUM(B3:D3)` 확인(기존 B2:D2에서 자동 이동). 값 유지.
- **차트 3개 모두 데이터 참조 유지·정상 표시.**
- 참고: 병합 셀의 가운데 정렬은 미반영(정렬 도구 한계, 05 TC-07과 동일).

## TC-07: 저장/영속 확인 — ✅ PASS
재오픈(에디터 새로 로드) 후 **막대·원형·꺾은선 차트 3개 + 표 + 병합 제목행 + SUM 총합값** 전부 영속. 소실·깨짐 없음.

## 이슈
- **차트 스타일 편집 미지원**(색/축 글자/격자선) — 차트 생성만 가능 — TC-05.
- 셀 가운데 정렬 미반영(병합 시) — 정렬 도구 부재 — TC-06 부분.

## 정책 참조
- `specs/` — XLSX 차트·셀 병합·수식 정책
